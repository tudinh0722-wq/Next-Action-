<?xml version="1.0" encoding="utf-8"?>
<!--
  Initial / fallback layout for the NextA home-screen widget.
  Uses only RemoteViews-compatible views (LinearLayout, TextView).
  The Flutter alarm_scheduler replaces these TextViews at runtime.
-->
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/widget_root"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:gravity="center_vertical"
    android:padding="12dp"
    android:background="@drawable/widget_background">

    <TextView
        android:id="@+id/widget_title"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="NextA"
        android:textSize="14sp"
        android:textStyle="bold"
        android:textColor="#FFFFFFFF"
        android:maxLines="1"
        android:ellipsize="end" />

    <TextView
        android:id="@+id/widget_subtitle"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="4dp"
        android:text="Không có sự kiện sắp tới"
        android:textSize="12sp"
        android:textColor="#CCE8F0FF"
        android:maxLines="2"
        android:ellipsize="end" />

    <TextView
        android:id="@+id/widget_countdown"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="6dp"
        android:text=""
        android:textSize="11sp"
        android:textColor="#99E8F0FF"
        android:maxLines="1"
        android:ellipsize="end" />

</LinearLayout>
